<li>
  {{#unless devoured}} 
  <h3>{{burger}} <button class="dvrDis" data-id="{{id}}">Devour!!!! </button> </h3>
  
  {{/unless}}
  {{#if devoured}}
  <h3>{{burger}} <button class="delDis" data-id="{{id}}">Delete Da-Burger!!!</button> </h3>

  {{/if}}
</li>